#include <stdio.h>
int main(void) {
    int a, b, c, d, e;
   printf("Podaj trzy liczby", &a, &b, &c);
   scanf ("%d, %d, %d", &a, &b, &c, &d);
if (a>b) {
d
}
else {
    d>c
e
    scanf "%d, %d, %d", &d, &c, &e
printf("Największa liczba to %d", &e)

}
   return 0;
}
